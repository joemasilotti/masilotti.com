---
title: Bridge Components - Native integrations for Hotwire Native apps
description: Build iOS and Android features with HTML, no Swift or Kotlin required.
layout: bridge_components
permalink: /bridge-components/
favorite: 1
ogkit: true
---

{% include bridge_components/og.liquid %}

{% assign pro = site.data.bridge_components.components | where_exp: "item", "item.pro" | size %}
{% assign free = site.data.bridge_components.components | where_exp: "item", "item.pro != true" | size %}

<header class="max-w-3xl text-center mx-auto mt-16 md:mt-20">
  <h1 class="text-5xl sm:text-6xl font-bold text-gray-900 flex flex-col items-center">
    <span class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary-50/75 text-primary-700 text-sm font-medium mb-4">Bridge Components</span>
    <span class="tracking-tight ">Native integrations for Hotwire Native apps</span>
  </h1>
  <p class="mt-6 text-xl sm:text-2xl text-gray-600 mx-auto leading-relaxed">Build iOS and Android features with HTML, <span class="italic block">no Swift or Kotlin required.</span></p>
</header>

<section id="components" class="mt-16 md:mt-24 scroll-mt-12">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10 md:gap-20 xl:-mx-10 mt-12">
    {% for component in site.data.bridge_components.components %}
      {% include bridge_components/component.liquid component=component %}
    {% endfor %}
  </div>
</section>

<section id="how-to" class="mt-24 scroll-mt-12">
  <h2 class="text-3xl md:text-4xl font-semibold tracking-tight text-gray-900 text-center">Add native features with HTML</h2>
  <p class="mt-4 text-base md:text-lg text-gray-600 max-w-lg mx-auto text-center leading-relaxed">Add data attributes to your Rails views to get fully native components on iOS and Android.</p>

  <div class="hidden md:block mt-16">
    <img src="/assets/images/bridge-components/hero.png">
  </div>

  <div class="md:hidden">
    <h2 class="text-2xl text-gray-800 font-medium mt-12">Add HTML data attributes</h2>
    <img class="mt-4" src="/assets/images/bridge-components/hero-mobile-1.png">
    <h2 class="text-2xl text-gray-800 font-medium mt-12">Get native components</h2>
    <img class="mt-4" src="/assets/images/bridge-components/hero-mobile-2.png">
  </div>
</section>

<section id="testimonials" class="mt-24 scroll-mt-12">
  <h2 class="text-3xl md:text-4xl font-semibold tracking-tight text-gray-900 text-center">What developers are saying</h2>
  <p class="mt-4 text-base md:text-lg text-gray-600 max-w-lg mx-auto text-center leading-relaxed">Rails developers are saving hours of work and shipping native features faster than ever.</p>
  <div class="mt-8 grid gap-6 md:grid-cols-2 md:-mx-6">
    {% for testimonial in site.data.bridge_components.testimonials %}
      {% include testimonial.liquid object=testimonial %}
    {% endfor %}
  </div>
</section>

<section id="pro" class="mt-24 scroll-mt-12 px-4">
  <div class="max-w-3xl mx-auto text-center">
    <h2 class="text-3xl font-bold tracking-tight text-gray-900">Unlock more native power with PRO</h2>
    <p class="mt-3 text-lg text-gray-600">Advanced components and priority support for production Hotwire Native apps.</p>
    <a href="{% link _pages/bridge_components/pro.liquid %}" class="inline-flex items-center gap-2 rounded-full border border-primary-200 bg-primary-50/60 px-5 py-3 text-base font-medium text-primary-700 shadow-sm transition hover:bg-primary-100/80 hover:text-primary-900 mt-6">
      {% svg "/assets/icons/outline/cube.svg" class="h-5 w-5 text-primary-700" %}
      Explore PRO
    </a>
  </div>
</section>

<section id="newsletter" class="mt-32 scroll-mt-18">
  {% include bridge_components/newsletter.liquid %}
</section>
