---
title: Services
description: I've been working with Hotwire Native since 2015. Since then, I've helped dozens of businesses launch their apps in the app stores. Here's how I can help you bring your Rails app to iOS and Android with Hotwire Native.
layout: simple
layout_title: Need a hand with Hotwire Native?<br>I'd love to help.
---

{% comment %} <div class="-mt-12 sm:-mt-[56px]"> {% endcomment %}
<div class="-mt-16 sm:-mt-20">
  <a href="mailto:joe@masilotti.com" class="button button-primary button-lg flex mt-4">
    {% svg /assets/icons/mini/envelope.svg class="-ml-0.5 h-5 w-5" %}
    Email Joe
  </a>
</div>

{% if site.testimonials %}
  <div class="flow-root mt-8 md:mt-12">
    <div class="-mt-8 sm:-mx-4 sm:columns-2">
      {% for testimonial in site.data.testimonials limit: 2 %}
        {% include testimonial.liquid object=testimonial %}
      {% endfor %}
    </div>
  </div>
{% endif %}

<ul class="flex max-w-xl flex-col space-y-16 mt-16">
  {% for service in site.data.services %}
    {% include service.liquid service=service %}
  {% endfor %}
</ul>

{% if site.testimonials %}
  <div class="flow-root mt-8 md:mt-12">
    <div class="-mt-8 sm:-mx-4 sm:columns-2">
      {% for testimonial in site.data.testimonials limit: 2 offset: 2 %}
        {% include testimonial.liquid object=testimonial %}
      {% endfor %}
    </div>
  </div>
{% endif %}
