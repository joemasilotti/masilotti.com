<p>When: Tuesday, October 17 at 10am-12pm PT on Zoom | Cost: $99 per person</p>

<% if available? %>
  <div class="not-prose">
    <div class="flex items-center">
      <%= render UI::Button.new(title: "Buy my ticket", href:, onclick: "fathom.trackGoal('#{event_id}', 0);") %>
      <p class="text-sm font-semibold leading-6 text-zinc-900 ml-6"><%= hero %></p>
    </div>
  </div>
<% else %>
  <p><span class="font-semibold">Sorry, this session is sold out.</span> The next session will take place in late 2023. Add your email to get first access when it goes on sale.</p>

  <div class="not-prose">
    <%= render EmailCapture.new(newsletter, cta: "Notify me") %>
    <p class="mt-4"><small>You’ll also be subscribed to the <a href="/hotwire" class="underline">Hotwire dev newsletter</a>.</small></p>
  </div>
<% end %>
