~~~ruby
front_matter do
  title "Joe Masilotti's newsletter"
  description -> { site.data.newsletters.masilotti.description }
  permalink "/newsletter/"

  image "/images/pages/newsletter.png"
  hide_newsletter true
end
~~~

<%= render UI::SimpleLayout.new(
  title: resource.data.title,
  intro: resource.data.description,
  newsletter: site.data.newsletters.masilotti
) do %>
  <div class="prose max-w-xl -mt-4">
    <blockquote>
      <p>Joe's newsletter is consistently the best 10 minutes I’ll spend all day. <br>- <span class="font-semibold">Alan Lattimore</span></p>
    </blockquote>

    <h2 class="mt-14">Content sneak peek</h2>
    <p>Some recent exclusives for my newsletter subscribers. 👀</p>

    <ul class="not-prose flex flex-col space-y-16 pl-0">
      <% site.data.exclusives.each do |exclusive| %>
        <%= render Exclusive.new(**exclusive.to_options) %>
      <% end %>
    </ul>
  </div>
<% end %>
