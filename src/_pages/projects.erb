---
title: Projects
description: |
  I've worked on a ton of projects over the years. Some sold and some abandoned.
  These are the ones I'm actively updating and improving. They are also the
  source for most of my content.
permalink: /projects/

---

<%= render UI::SimpleLayout.new(
  title: "The businesses and projects that I'm working on day to day.",
  intro: resource.data.description
) do %>
  <ul role="list" class="grid grid-cols-1 gap-x-12 gap-y-16 sm:grid-cols-2 lg:grid-cols-3">
    <% site.data.projects.each do |project| %>
      <%= render Project.new(**project.to_h.to_options) %>
    <% end %>
  </ul>
<% end %>
